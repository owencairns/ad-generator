import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: [
      "lh3.googleusercontent.com",
      "images.unsplash.com",
      "storage.googleapis.com",
      "ad-generator-570e6.firebasestorage.app",
    ],
  },
};

export default nextConfig;
